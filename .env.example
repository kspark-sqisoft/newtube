# .env.local 로 복사한 뒤 값을 채우세요. 실제 비밀 값은 커밋하지 마세요.

# -----------------------------------------------------------------------------
# 데이터베이스 (Neon Postgres)
# https://neon.tech — Drizzle 마이그레이션·앱 런타임 공통 연결 문자열
# -----------------------------------------------------------------------------
DATABASE_URL=

# -----------------------------------------------------------------------------
# 앱 공개 URL
# SSR·서버 컴포넌트에서 tRPC 호출 시 절대 URL 베이스로 사용
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# Clerk (인증)
# https://dashboard.clerk.com — API 키 + 웹훅 서명 비밀값
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# 웹훅 엔드포인트: /api/users/webhook
CLERK_SIGNING_SECRET=

# -----------------------------------------------------------------------------
# UploadThing (이미지 업로드)
# https://uploadthing.com/dashboard — 대시보드에서 발급하는 토큰(base64 JSON)
# -----------------------------------------------------------------------------
UPLOADTHING_TOKEN=

# -----------------------------------------------------------------------------
# Mux (동영상 스트리밍)
# https://dashboard.mux.com — API 액세스 토큰 + 웹훅 서명 비밀값
# 웹훅: /api/videos/webhook (프로덕션·로컬 테스트는 공개 URL 필요, 예: ngrok)
# -----------------------------------------------------------------------------
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
MUX_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Upstash Redis (레이트 리밋 — protectedProcedure)
# https://console.upstash.com
# -----------------------------------------------------------------------------
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# -----------------------------------------------------------------------------
# Upstash Workflow / QStash
# https://console.upstash.com — src/lib/workflow.ts·영상 프로시저에서 사용
# UPSTASH_WORKFLOW_URL: QStash가 워크플로 단계를 POST할 수 있도록 이 앱의 공개 베이스 URL
# (예: https://도메인 또는 개발 시 ngrok URL)
# -----------------------------------------------------------------------------
QSTASH_TOKEN=
UPSTASH_WORKFLOW_URL=http://localhost:3000

# -----------------------------------------------------------------------------
# OpenAI — 영상 워크플로: 제목·설명·썸네일 등
# -----------------------------------------------------------------------------
OPENAI_API_KEY=
