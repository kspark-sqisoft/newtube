name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint
        run: bun run lint

      - name: Type check
        run: bunx tsc --noEmit

      - name: Test
        env:
          # CI 에선 외부 서비스 없이 단위 테스트만 실행
          DATABASE_URL: "postgresql://test:test@localhost:5432/test"
          CLERK_SECRET_KEY: "test-secret"
          CLERK_SIGNING_SECRET: "test-signing"
          MUX_TOKEN_ID: "test"
          MUX_TOKEN_SECRET: "test"
          MUX_WEBHOOK_SECRET: "test"
          UPSTASH_REDIS_REST_URL: "https://example.upstash.io"
          UPSTASH_REDIS_REST_TOKEN: "test"
          QSTASH_TOKEN: "test"
          UPSTASH_WORKFLOW_URL: "https://example.com"
          OPENAI_API_KEY: "test"
        run: bunx vitest run

      - name: Build
        env:
          DATABASE_URL: "postgresql://test:test@localhost:5432/test"
          CLERK_SECRET_KEY: "test-secret"
          CLERK_SIGNING_SECRET: "test-signing"
          MUX_TOKEN_ID: "test"
          MUX_TOKEN_SECRET: "test"
          MUX_WEBHOOK_SECRET: "test"
          UPSTASH_REDIS_REST_URL: "https://example.upstash.io"
          UPSTASH_REDIS_REST_TOKEN: "test"
          QSTASH_TOKEN: "test"
          UPSTASH_WORKFLOW_URL: "https://example.com"
          OPENAI_API_KEY: "test"
          # Clerk publishable key 는 "pk_(test|live)_<base64(`<frontendApi>$`)>" 형태여야 검증 통과
          # decode: "clerk.example.com$" — 빌드 시점 prerender 만 통과시키기 위한 더미 값
          NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: "pk_test_Y2xlcmsuZXhhbXBsZS5jb20k"
          NEXT_TELEMETRY_DISABLED: "1"
        run: bun run build
